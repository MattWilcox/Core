5_mockup

NOTE: This directory is usually set up as a website running under http://projectname.mockup.local

Some projects (in particular ones using Perch CMS) are most efficiently built as flat HTML/CSS first, and a CMS added 'on top' later. Flat HTML/CSS implementations live in this directory.